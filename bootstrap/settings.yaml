apiVersion: deployments.plural.sh/v1alpha1
kind: DeploymentSettings
metadata:
  name: global
  namespace: plrl-deploy-operator
spec:
  managementRepo: Soot3/plurallover

  stacks:
    jobSpec:
      namespace: plrl-deploy-operator
      serviceAccount: stacks
